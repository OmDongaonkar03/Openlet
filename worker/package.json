{
  "name": "openlet-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create openlet",
    "db:migrate:local": "wrangler d1 migrations apply openlet --local",
    "db:migrate:remote": "wrangler d1 migrations apply openlet --remote"
  },
  "dependencies": {
    "@elithrar/workers-hono-rate-limit": "^0.5.1",
    "hono": "^4.4.0"
  },
  "devDependencies": {
    "wrangler": "^3.60.0"
  }
}
