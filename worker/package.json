{
  "name": "openlet-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create openlet",
    "db:migrate:local": "wrangler d1 execute openlet --local --file schema.sql",
    "db:migrate:remote": "wrangler d1 execute openlet --file schema.sql"
  },
  "dependencies": {
    "hono": "^4.4.0"
  },
  "devDependencies": {
    "wrangler": "^3.60.0"
  }
}